{
  "version": "0.2.0",
  "configurations": [
    {
      "args": [
        "--disable-extensions",
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/private/test_project"
      ],
      "name": "Launch Extension",
      "outFiles": [
        "${workspaceFolder}/out/bun/**/*"
      ],
      "preLaunchTask": "bun: build",
      "request": "launch",
      "type": "extensionHost"
    }
  ]
}
